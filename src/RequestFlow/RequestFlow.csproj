<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Request/handler mediator for .NET with Task and ValueTask dispatch, startup validation, application-defined validation rules, stages, streaming, events, and frozen dispatch plans. MIT, with no compiler plugin or generated dispatch code.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="8.0.1" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\RequestFlow.Abstractions\RequestFlow.Abstractions.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="RequestFlow.Tests.Unit" Key="$(RequestFlowPublicKey)" />
  </ItemGroup>

</Project>
