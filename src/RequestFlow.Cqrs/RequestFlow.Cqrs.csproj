<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Type-enforced CQRS for RequestFlow with command, query, and stream-query dispatchers plus startup validation through AddCqrs. Includes the RequestFlow runtime.</Description>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="8.0.1" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\RequestFlow.Cqrs.Abstractions\RequestFlow.Cqrs.Abstractions.csproj" />
    <ProjectReference Include="..\RequestFlow\RequestFlow.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="RequestFlow.Cqrs.Tests.Unit" Key="$(RequestFlowPublicKey)" />
  </ItemGroup>

</Project>
