<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Type-enforced CQRS for RequestFlow with Task and ValueTask command/query dispatchers, stream-query dispatch, and startup validation through AddCqrs. Includes the RequestFlow runtime.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\RequestFlow.Cqrs.Abstractions\RequestFlow.Cqrs.Abstractions.csproj" />
    <ProjectReference Include="..\RequestFlow\RequestFlow.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="RequestFlow.Cqrs.Tests.Unit" Key="$(RequestFlowPublicKey)" />
  </ItemGroup>

</Project>
