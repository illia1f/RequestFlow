<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>CQRS contracts for RequestFlow: ICommand, IQuery, IStreamQuery, handler and dispatcher interfaces. No runtime dependency, safe to reference from domain and application layers.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\RequestFlow.Abstractions\RequestFlow.Abstractions.csproj" />
  </ItemGroup>

</Project>
