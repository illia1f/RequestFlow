<?xml version="1.0" encoding="utf-8"?>
<RunSettings>

  <RunConfiguration>
    <!-- A leg that discovers no tests exits zero without this, so a runner that fails to load looks green. -->
    <TreatNoTestsAsError>true</TreatNoTestsAsError>
  </RunConfiguration>

</RunSettings>
